# fetch-it-all
fetch-it-all is an npm package that takes an array of urls and returns data in json format

# Installation
Use npm to install the package

```
npm install fetch-it-all

```
# Usage

```
import requestMultipleURLs from 'fetch-it-all';
require('fetch-it-all');

```
